apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - rbac.yaml
  - deployment.yaml

# Decrypts and emits the tailscale-auth Secret via KSOPS.
generators:
  - ./secret-generator.yaml

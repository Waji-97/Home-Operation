extends: relaxed

rules:
  line-length: disable
  document-start: disable
  comments:
    min-spaces-from-content: 1
  truthy:
    check-keys: false
  indentation:
    spaces: 2
    indent-sequences: consistent

ignore: |
  kubernetes/apps/*/charts/
  *.sops.yaml
